# semantic-ci target.yaml — Phase 1.5 safe-stats helper consolidation
intent: "Move float|None safe_rate/safe_mean/safe_median into metrics_utils with an ndigits parameter; drop duplicate locals in monthly_review and reporting. weekly_reports_v2's str-returning CSV-format helpers stay local. No behavior change."
change:
  primary_kind: refactor
  allowed_secondary_kinds:
    - test_update
  scope:
    files: []
    modules:
      - ugh_quantamental.fx_protocol.metrics_utils
      - ugh_quantamental.fx_protocol.monthly_review
      - ugh_quantamental.fx_protocol.reporting
authorship:
  authors:
    - identity: "claude-code-refactor-phase1-5"
  declared_at: "2026-05-08T00:00:00Z"
# All affected names are private (leading-underscore) helpers; the public API
# of fx_protocol is unchanged. The new public additions in metrics_utils are
# the canonical home for these helpers, declared explicitly below.
api_surface:
  allow_changes:
    - fqn: "fx_protocol.metrics_utils.safe_rate"
    - fqn: "fx_protocol.metrics_utils.safe_mean"
    - fqn: "fx_protocol.metrics_utils.safe_median"
constraints: []
