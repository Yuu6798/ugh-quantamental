# semantic-ci target.yaml — Phase 3b annotation_coverage extraction
intent: "Extract duplicated annotation-status counting from weekly_reports_v2.build_annotation_coverage and monthly_review.compute_annotation_coverage_summary into fx_protocol/annotation_coverage.count_by_annotation_status. Both wrappers retain their existing public signatures and output dict shapes. No behavior change."
change:
  primary_kind: refactor
  allowed_secondary_kinds:
    - test_update
  scope:
    files: []
    modules:
      - ugh_quantamental.fx_protocol.annotation_coverage
      - ugh_quantamental.fx_protocol.weekly_reports_v2
      - ugh_quantamental.fx_protocol.monthly_review
authorship:
  authors:
    - identity: "claude-code-refactor-phase3b"
  declared_at: "2026-05-08T00:00:00Z"
# The two existing public wrappers (build_annotation_coverage,
# compute_annotation_coverage_summary) keep their signatures and
# output dict keys verbatim. The new helper is the canonical home.
api_surface:
  allow_changes:
    - fqn: "fx_protocol.annotation_coverage.count_by_annotation_status"
constraints: []
