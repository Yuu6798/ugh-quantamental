# STATUS - ugh-quantamental

最終更新: 2026-09-13

このファイルは日次の project snapshot として、現在フェーズ、次の発行順序、直近 merged を保持する。安定方針は `CLAUDE.md` / `AGENTS.md` に置き、canonical な milestone 表は `PLANS.md`、フェーズ計画は `docs/engine_review_2026_05_planning.md` / `docs/specs/` を参照する。

他の doc から live tracker を指す場合は、このファイルの `## 次の発行順序` にリンクする。

## Phase

Milestones 1-18 完了、engine default **v2.6** (FX-RANGE-DECOUPLE #122)。2026-08 月次レビューの briefs 4 本 (GOV-FLAT-AWARE / OUTCOME-CATCHUP / ESTAR-LAG / PRICE-ALERT) は PR #125 で全実装・main 反映済 (8/31 merge)。ESTAR-LAG の実データ ablation で SMA20 飽和仮説は棄却、momentum_5d が一貫した律速 (−2〜−10 営業日) — ただし 9/12 週報の査読で **momentum_5d は e_star 入力の上流にある 1 要素にすぎず、FLAT の機序を語るには成分分解が先**と確定 (queue 2)。9/5 週報 (#128) で catch-up 再発行の二重計上を検出・修正し、202608 governance を 9/6 に再生成 (ユーザー承認、dispatch report_date=20260901、**8 月データの実体は生成月キーの `monthly/202609/`**): **`inspect_magnitude_mapping` が新規発火** (UGH 平均絶対終値誤差が random_walk より 5.5bp 悪化、閾値 5.0)、random_walk の Close Err Delta −3.89 → −5.53bp、方向 slice 崩落も深刻化 (regime trending 39→32%、volatility normal 31→27%)、`inspect_direction_logic` は非発火のまま。9/12 週報 (#129、Codex 14 rounds / 24 件全採用) で **金曜 business-day guard 赤の実害が 2 つ確定**: CI 赤の常態化に加え、同じ run が週次 artifact の producer であるため Saturday 付 artifact が 08-29 以降 3 週連続で欠落 (4/18〜8/22 は毎週生成)。guard は Step 1 で raise するため exit 0 化では直らず、金曜 as_of で週次ブロックまで到達させる必要がある。運用: cron :23/:37 移動 (#127) の効果は GitHub 側の広域遅延 (今週 3h40〜6h10) が続き未判定。**guard as_of 修正は 9/13 に PR #130 で実装・merge 済** (非営業日着地時、前営業日の batch が完備していればその日へ繰り越して週次ブロックまで到達)。**PR #131 (9/13 merge) で実装可能なフォローアップを一括処理**: catch-up 書き側の二重 publish 修正 (#128 破損の発生源。spec の「END-date dir は正常 run では先在しない」という前提自体が誤りだった)、`analyze_estar_lag.py` の前提 4 件 (9 月分解が実行可能に)、stC の scoreboard rollup、annotation_source の純関数化、governance spec バナー訂正。Codex 10 rounds / 19 件全採用。次: レンジ幅較正 brief → 9/7 FLAT の成分分解 (点予測 magnitude 論点の再開を含む)。

## 次の発行順序

active queue - 未着手または進行中の Phase / Brief / Milestone のみを置く。終了した項目は wrap-up step 4 で `## 直近 merged` に移す。

1. **レンジ幅較正の brief 化 — 着手可** - 観測 2 週分が揃った。今週の使用率は的中 4 日で半幅の 29〜61%、9/7 の救済には +43% が必要。ただし**一様 1.43 倍でも 5 窓すべて包含されるため「一様スケールでは解けない」は撤回済**。**brief の第 1 課題は「緩すぎる幅を棄却する sharpness 基準の定義」**で、基準を決めてから一様スケールと条件付き幅を同一尺度で比較する。条件付き幅は仮説のまま。
2. **9/7 FLAT の成分分解 (9 月月次・最優先)** - epsilon は **固定 3.0bp**。FLAT 条件は `|e_star| × trailing_mean_abs_close_change_bp × (0.5+0.5×conviction) ≤ 3.0bp`。**PR #131 で前提の道具立ては完了** (窓の CLI 化 / `pre_expansion_close_change_bp` と 3 成分の出力 / `estar_term` 軸 = `compute_e_raw`・`compute_gravity_bias` が実際に受け取る 8 項の isolation / 全 variant 日次系列)。順序: **(i)** 9/4→9/7→9/8 の `pre_expansion_close_change_bp` を e_star / trailing 幅 / conviction に分解 → **(ii)** e_star が主因の場合のみ `estar_term` 軸で項を特定 (`fundamental_score` / `technical_score` は上流であって項ではない、と軸が分離済) → **(iii)** epsilon × 当該パラメータ。**道具の注意**: `neutral` 参照は項ごと (alignment=1.0 / fire_probability=0.5、0.0 は退化介入)、同順位は全件報告、baseline に転換日が無い variant は `null` — 9 月窓は α・γ・δ が `NO_POST_SHOCK_RECOVERY` なので β 以外は答えが出ない。
3. **点予測 magnitude の再点検 (9 月月次)** - 再生成後の 8 月 governance で `inspect_magnitude_mapping` が新規発火。**レンジ幅較正 (queue 1) とは別軸**で混ぜない — 幅を広げても点予測誤差は縮まず、逆に governance 候補 CC-001 自身が `expected_risk` に range_hit への影響を挙げている。7 月に「代替 5 案が改善せず据え置き」とした magnitude 論点を、二重計上を除いた訂正データで再開する。同時に深刻化した slice 崩落 (regime trending 32%、volatility normal 27%) も同一原因なので同じ入力に含める。
4. **regime=choppy の判定保留を継続** - 標本ゼロ 11 週目。intervention_risk 非 low 日は 44 obs 中 41 miss。label は move-size 由来、「大変動日に弱い」と読む。
5. **売買 / execution レイヤーの planning doc 起草** - conviction は e_star 符号整合時のみ信頼可 (2026-08 findings §1) — sizing 入力設計はこの条件付けを織り込む。
6. **follow-up (低優先)** - **PRICE-ALERT の sticky 挙動は open のまま** (`_apply` は `was_active` の間 re-fire を抑止し `True→False→True` でしか再武装しない。baseline 更新は再武装しないので 9/2–9/3 型の連続スライドは 1 通知に畳まれる。「日次 baseline 更新時に強制再武装」案を 9 月月次で検討)。グリッド方針 (B7) は **2026-08-30 ユーザー判断で終了 — 追跡・エスカレーション対象外**。

## 直近 merged

最新 5 件のみ inline。超過分は `archive/STATUS_MERGED_LOG.md` 末尾へ移す。

- **PR #131 / 実装可能フォローアップの一括処理** (2026-09-13) - catch-up 書き側 / replay script の前提 / governance バナー / stC rollup / 純関数化 を 1 PR で。**(1) catch-up 書き側**: 完備判定が catch-up 専用 dir しか見ず、通常評価済み窓を毎 run 再 publish → 読み手が二重計上 (#128 破損の発生源)。END-date dir も publish 済みと認める。判定は**内容検証 7 条件**へ (outcome_id 一致 / evaluation 行数 / 行の forecast_id 集合一致 / forecast 行の archive / `strict=True` / 構造的短長行の棄却 / 両ヘッダが reader の index 列を持つ) — 各条件が単独で破れることを unit test で固定。**発生源は spec の記述**「END-date recovery dir は正常 run では先在しない」で、候補条件が距離 `>= 1` である以上これは誤り。spec 訂正済。**(2) `analyze_estar_lag.py`**: 窓の CLI 化 / `pre_expansion_close_change_bp` と 3 成分 / `estar_term` 軸 (compute_e_raw・compute_gravity_bias の実引数 8 項を消費点で置換、`compute_u` は常に元 features) / 全 variant 日次系列。**(3)** stC rollup + spec、**(4)** `resolve_annotation_source` 純関数化、**(5)** governance spec を Implemented へ。Codex **10 rounds / 19 件全採用**、却下 1 (provider_health 列追加 — CSV スキーマ変更が週次・月次に波及)。**自分の誤りの訂正 3 件**: 「one term of e_star」は成立せず (compute_u が混ぜる)、`alignment` neutral=0.0 は退化介入で「alignment が律速」は所見でなくアーティファクト、2026-08 の β×neutral は tie を単独名で報告していた。テスト 4 本は「修正なしで通る」状態から書き直し。2026-08 回帰: 統計軸 18 行・alpha 日次 27 行が byte-identical。
- **PR #130 / business-day guard の as_of 繰り越し** (2026-09-13) - #129 で確定した実害 2 つ (毎週金曜の CI 赤 + Saturday 週次 artifact の 3 週欠落) の恒久対策。非営業日着地時、**前営業日の forecast batch が完備している場合に限り**その日へ繰り越して継続 → 週次ブロック到達 → exit 0。完備は既存基準 (`make_forecast_batch_id` → `load_fx_forecast_batch` → `EXPECTED_DAILY_BATCH_SIZE`) を流用。batch 不在・不完全は本物の欠測として従来どおり raise。繰り越し先は前**営業**日なので日曜着地も金曜に落ちる (安全性の担保は gap 幅ではなく batch 完備)。Codex 2 件 (うち P1 1 件) 全採用: (1) Step 7 の provider lag が wall-clock 基準のため救済 run が `provider_health.csv` に偽 lag 行を積み、同 run が生成する週報・月次が数えてしまう → 実際に問い合わせた as_of 基準へ。(2) **P1**: provider 退行時に通常の 1 日 fallback が金曜→木曜へさらに戻し、`latest/` を古い日で上書きしつつ週次ゲートからも外れる → 繰り越し後は後退を拒否して raise。却下 1 件 (carry-over 専用列の追加 — CSV スキーマ変更が週次・月次の読み手に波及、warning ログと `run_status` で識別可能)。テスト 5 本、各々「自分の修正だけを戻すと落ちる」ことを確認。3 rounds で収束。
- **PR #129 / 9/7–9/11 週報 + skill 診断手順の再構成** (2026-09-12) - BoJ 9 月利上げ観測で 152.87 (7 か月ぶり安値)、UGH 方向 2/4・Range 3/4。**週最大の下げ日 (9/7、−102.0bp) だけ全 variant FLAT**、翌日 15 発行ぶりの `fire` 点火。epsilon は動的ではなく **固定 3.0bp** と確定。**金曜 guard 赤の実害が 2 つ確定** (CI 赤 + Saturday 付 artifact 3 週欠落) し queue 最優先へ。前週持ち越しの 202608 governance 再生成結果 (`inspect_magnitude_mapping` 新規発火) をレポートに回収し、点予測 magnitude を独立 queue 化。skill は週次 trigger の **4 節すべて**で診断する形に再構成 (3 節は健全な run でも落ちる)。Codex 14 rounds / 24 件全採用、境界宣言 (round 11 以降は critical bug / 実コード破壊 / 将来汚染) 後の 4 件はすべて「将来汚染」として境界内判定。訂正の主因は自分の機序誤認 (guard の分岐、volatility ラベルの入力、PRICE-ALERT の再武装条件)。
- **PR #128 / 8/31–9/4 週報 + labeled_observations 二重計上修正** (2026-09-05) - 9/2–9/3 ショック週 (−272bp、UGH 方向 1/4・Range 2/4)。発行方向の転換は β 1 / α・γ・δ 2 営業日 (8 月の 6 / 14 と同じ物差し、順序同一)。初運用: catch-up が 8/27 batch を回収、price-alert が Issue #126。**実害修正**: `_collect_labeled_observation_rows` (labeled_observations.csv の書き手) に forecast_id dedupe が無く、catch-up 再発行 (8/21〜9/2) を週次・月次・governance が二重計上 (週次 7 obs / 正 4、**202608 governance は要再生成**)。regression test 付き。skill 硬化 (Obs 期待値は distinct 評価 window 数から導出、detail helper は as_of キー + recovered 行の表示規則)。Codex 5 rounds / 5 件全採用、境界宣言は発動前に収束。
- **PR #127 / daily-protocol cron の :23 移動** (2026-09-03) - ops-only。GitHub Actions の毎時 0 分 schedule が 8/28 (欠測) / 8/31 / 9/1 (手動 dispatch で救済) と 3 営業日連続で遅延・欠落したため、daily cron 3 本を :23 へ、監視側 price-alert cron を :37 へ移動。`FX_LAST_RETRY` の cron 文字列一致も同期 (見落とすと最終 retry の fail-hard が静かに外れる)。self-review で spec の猶予算術誤り (20:23+2h≠22:00) を訂正し、旧時刻の記述 6 箇所を同期、daily script のコメントは時刻非依存化。Codex 2 rounds (round 1 = 2 件、いずれも self-review で先回り済み / round 2 = 指摘なし)。境界宣言 (round 11 以降は critical bug / 実コード破壊 / 将来汚染のみ) を PR に掲示、発動前に収束。
