# 2026-05-31

## コンテキスト

`semantic-ci-code` と `ugh-quantamental` の 2 リポジトリ接続を確認したのち、
ユーザー依頼で `semantic-ci-code` の開発フロー (設計/実装分離)・セッション終了
プロトコル・経験値外部化スキームを `ugh-quantamental` に**ローカライズ移植**した。
単純コピーではなく、本リポの architecture invariants・`src/ugh_quantamental/`
構成・bullet 形式 `_index.md`・PEP 735 `[dependency-groups]` に合わせて翻訳。
PR #106 として作成し、Codex P2 review 3 件を全対応してマージ。

本セッションは移植した wrap-up プロトコル自身の初 dogfood でもある。

## 設計判断

- **フル移植を選択** (ユーザー確認: フル / コア / 最小の 3 択)。AGENTS.md
  handoff protocol + Task Brief/Completion Summary + STATUS.md + 8-step
  wrap-up + 2 skills + session-start hook + Experience Externalization +
  tiered Required Reading + `tests/discipline/` まで一式。
- **ローカライズ方針**: semantic-ci の §23.1 input neutrality → ugh の
  architecture invariants (pure engine / frozen schemas / naive-UTC / import
  isolation / read-only replay / review-audit boundary) に remap。`/new-brief`
  §1a の grep 対象を `domain/state_schema.py` 等 → `src/ugh_quantamental/`
  配下に写像。empirical base case は PR #82/#84 → 本リポの PR #104 (13 rounds)
  に読み替え。
- **`--no-cov` 除去**: 本リポは `pytest-cov` 未導入のため wrap-up gate を
  `python -m pytest tests/discipline/` に簡素化。
- **session-start hook**: `pip install -e ".[dev]"` は PEP 735 dev group を
  解決しないため、`pip install -e . ruff pytest` に変更。
- **STATUS.md の Phase は engine work に限定**: governance 移植 (PR #106) は
  infra なので Phase 段落は engine_review の P0/P1/P2 のまま、直近 merged に
  記録するに留めた。

## 成功パターン

- **移植前に両 repo を全読**: semantic-ci の CLAUDE.md / AGENTS.md / 2 skills /
  hook / settings.json と、ugh の現状 (既に軽量 Session Memory が部分移植済) を
  突き合わせ、未移植分を差分として特定してから着手。
- **discipline test を書いた直後にローカル実行で緑を確認**してから push。
- **自分の grounding 修正自体を grep で裏取り** (新-brief のパス修正時、13 パス
  全実在を確認) — checklist の趣旨を自分に適用。

## 修正・訂正

Codex P2 review 3 件、すべて同根 (**Markdown の継続行を走査しない**バグ):

- **#1 (`8945822`)**: `_index.md` compactness gate が bullet の 1 物理行しか
  測らず、インデント継続行の essay がすり抜ける → bullet ブロック全体を結合して
  500 字 cap。
- **#2 (`de7be34`)**: `/new-brief` §1a が「repo-root relative」と書きつつ
  `schemas/` 等の top-level package 名を列挙 (実体は `src/ugh_quantamental/`
  配下) → 全パスにフルプレフィックス付与、13 パス実在を検証。
- **#3 (`f0be9ca`)**: queue gate が複数行 numbered item の継続行を skip し、
  継続行の `merged`/`完走` マーカーを見逃す → list item を継続行ごと block 化
  して走査。#1 と同じ block-grouping パターンに統一。

残る `test_status_md_phase_single_paragraph.py` は `\n\n` 分割で段落全体を見て
いるため同種の穴なし。

## 工程サマリー

| 工程 | 内容 | コミット |
|---|---|---|
| 接続確認 | 2 repo (semantic-ci-code / ugh-quantamental) を確認 | — |
| 調査 | 両 repo の governance 資産と移植先の不足分を特定 | — |
| 移植実装 | AGENTS.md / CLAUDE.md / STATUS.md / 2 skills / hook / settings.json / tests/discipline | `5c21f1c` |
| Review #1 | compactness gate を block 測定に | `8945822` |
| Review #2 | new-brief §1a を実パスに | `de7be34` |
| Review #3 | queue gate を block 走査に | `f0be9ca` |
| マージ | PR #106 main へ (`d5b8ef6`) | — |

## 成果物

**マージ済み PR**: #106 — governance 移植 (`d5b8ef6`)

**新規ファイル**:
- `.claude/memory/STATUS.md`
- `.claude/skills/wrap-up/SKILL.md`, `.claude/skills/new-brief/SKILL.md`
- `.claude/hooks/session-start.sh`, `.claude/settings.json`
- `tests/discipline/` (`__init__.py` / `_helpers.py` / `README.md` /
  `test_status_md_phase_single_paragraph.py` /
  `test_status_md_next_queue_no_completed.py` /
  `test_index_md_entry_compactness.py`)

**変更ファイル**: `AGENTS.md` (handoff protocol 追加)、`CLAUDE.md`
(Current status / Workflow / tiered reading / 8-step wrap-up / Archive policy /
Experience Externalization)

## 次セッションへの引き継ぎ

- **移植した protocol が本番稼働**: 次回以降、終了フレーズで `/wrap-up`、brief
  起草で `/new-brief` が使える。SessionStart hook は `CLAUDE_CODE_REMOTE=true`
  の web セッションでのみ依存をインストール。
- **engine work は未着手のまま**: STATUS.md 次の発行順序 Phase 1〜6 (range_hit
  集計修正 → state sharpening → FLAT epsilon → range per-variant → spec 改訂 →
  M18 monthly) は移植作業と独立。次の実装着手は Phase 1。
- **brief 駆動を試す好機**: Phase 1 を `/new-brief` で Task Brief 化して Codex
  に渡す運用が、移植した分離スキームの初の実戦テストになる。

## メモ

- **Codex の chained findings パターンが再現**: PR #104 memo の「ひとつの修正が
  次の不整合を表面化させる」と同じく、今回は「1 行目しか見ない」系を
  compactness → grounding → queue と 3 連続で指摘。誠実に毎 round 対応 +
  block-grouping に統一。
- review は 3 round だが各々 1 件で完結 (5+ round の曖昧 spec なし) のため、
  wrap-up step 7 の追加 externalize は不要。指摘内容はテスト自身に encode 済。
- 移植元 `semantic-ci-code` は読み取りのみ、一切変更していない。
