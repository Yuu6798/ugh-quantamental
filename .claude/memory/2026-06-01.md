# 2026-06-01

## コンテキスト

2026-05 engine review の Phase 1 から Phase 3A までを小 PR で順に処理した。PR #107 は `range_hit` の per-batch 集計を `ugh_v2_ensemble` に寄せ、PR #108 は mixed `engine_version` window の auto-stratify を追加した。PR #109 は state classifier の v2.1 bump、PR #110 は UGH variant 限定の rare FLAT epsilon と v2.2 bump。

## 設計判断

`engine_version` bump は出力境界が変わる箇所だけに限定した。P2B では fire 到達性と winner-margin を分離するため、fire evidence は weighted sum + catalyst floor、最終 `updated_probabilities` は独立温度 `final_softmax_temperature=0.12` の softmax 正規化にした。P3A は dynamic ratio sweep を経て、UGH の予測自体が volatility-scaled であるため default は固定 3.0bp epsilon (`ratio=0.0`, `floor=3.0`) に確定した。

## 成功パターン

PR ごとに acceptance を狭く保ち、replay sweep の数値は PR 本文に置き、脆い rate threshold は unit test に固定しなかった。review comment は原因を caller override preservation に絞って小さく修正した。

## 修正・訂正

P2B の第一レバー (`prior_weight` / `evidence_weight`) は fire と winner-margin のトレードオフで blocked と判断し、第二レバーの最終層 softmax に切り替えた。P3A の当初 acceptance にあった 5/26 type 救済は magnitude calibration の領域として外し、rare FLAT 運用に再定義した。

## 工程サマリー

| PR | 内容 | 結果 |
| --- | --- | --- |
| #107 | ENGINE-P1A range_hit per-batch aggregation + `ugh_v2_ensemble` | merged |
| #108 | ENGINE-P2A report_window `engine_version` auto-stratify | merged |
| #109 | ENGINE-P2B state classifier sharpening + v2.1 bump | merged |
| #110 | ENGINE-P3A rare FLAT epsilon + v2.2 bump | merged |

## 成果物

- `docs/engine_review_2026_05_planning.md` の Phase 1-3A に対応する実装 PR 一式。
- `docs/specs/ugh_state_engine_v1.md` に二段温度と fire gate の意図を反映。
- `docs/specs/fx_ugh_engine_v2.md` に UGH variant 限定 FLAT epsilon、baseline 据え置き、5/26 type の scope 外整理を反映。

## 次セッションへの引き継ぎ

次の主候補は Phase B: range per-variant scoreboard。`expected_range` を variant-specific にする Phase B は、P1A の `ugh_v2_ensemble` synthetic row と衝突しないよう、raw forecast/evaluation schema と monthly/weekly aggregation の境界を先に確認する。

## メモ

5 round 以上続いた論点は P2B winner-margin と P3A FLAT epsilon。どちらも最終判断を spec と tests に外部化済みで、現時点で `CLAUDE.md` / `AGENTS.md` への追加提案は不要。

---

# 2026-06-01 (Session 2)

## コンテキスト

engine review 2026-05 の残りを締めた。Phase B を壁打ちで設計確定 → ブリーフ起草 → Codex 実装 (PR #111) をレビューして merge。続いて Phase 4 (conviction spec) を壁打ち + ブリーフ → PR #112 merge。最後に Milestone 18 (monthly FX reporting) が **既に完全実装済み**であることを発見し、PLANS.md を実態に同期した。

## 設計判断

Phase B は range の出どころで 3 案 ((A) projection width 一本化 / (B) 新式 / (C) 最小デルタ) を比較し (A) を採用。中心は非 FLAT で expected close に recenter、FLAT は spot 中心対称。conviction が中心 magnitude と幅の両方に入る件は、点予測の式が scope 外のため「役割分離」を諦め、B1「一貫した uncertainty model として spec 明記」に確定。較正は `range_width_scale` + 半幅 floor を新設し replay で着地 (scale=2.0 → 71.7%)。Phase 4 §8 dormant↔magnitude は Option B (decouple、売買層に委譲) で確定、acceptance の range_hit ゾーンは「暫定較正値」と明記。

## 成功パターン

壁打ちの前にコードで接地してから fork を提示したことで churn ゼロ。`/new-brief` の grounding gate が stale 参照を 2 件捕捉 (epsilon default 0.0 vs planning ドラフト 0.5、`compute_conviction` 行番号、#111 後の magnitude scaler 行移動)。PR #111 のレビューは acceptance 二層化 (Hard=pytest / Soft=replay 報告) が効き 0 review round で landing。

## 修正・訂正

PR #111 レビューで当初「state.py / report_window.py が scope 外変更」と誤検知したが、原因はローカル `main` が stale だったこと。`origin/main` 基準で diff し直して誤りを訂正、実 scope はブリーフ通りと確認。Milestone 18 は「未着手」と思い込まず PLANS の "Next up" を疑って接地した結果、既存実装 (891行 + spec 2本 + workflow 2本 + test 1745行) を発見。

## 工程サマリー

| 項目 | 内容 | 結果 |
| --- | --- | --- |
| PR #111 | ENGINE-P3B variant-specific expected_range + v2.3 bump | merged |
| PR #112 | ENGINE-P4 conviction 意味論明文化 + §8 Option B 記録 (docs-only) | merged |
| M18 検証 | monthly review 既存実装の end-to-end スモーク + test 確認 | 完成確認 |
| PLANS 同期 | M18 を完了表に追加、Next up を実態化 | branch push (PR pending) |

## 成果物

- PR #111 / #112 (engine review 2026-05 を全クローズ: P0-P4 + Phase B)。
- `docs/specs/fx_ugh_engine_v2.md` §5.1.2 (range from projection width) / conviction reliability 段落 / §8 Option B。
- PLANS.md: Milestone 18 を完了 milestone に追加、Next up を「engine 前提整備完了、次は売買レイヤー planning」に更新 (branch `claude/remaining-tasks-review-YkIQi`)。

## 次セッションへの引き継ぎ

(1) PLANS.md M18 同期を PR 化 → merge。(2) governance spec のバナーが `Status: Draft` だが実装+test+workflow 完備なので実態確認のうえ更新検討。(3) 次の大方向は売買 / execution レイヤーの planning doc 起草 — engine 出力 (state / conviction / expected_close_change_bp / expected_range) を入力にした position sizing。大規模新スコープなので spec/planning から。

## メモ

PLANS.md / STATUS の queue は実装の実態から drift しうる (M18 は完成済みなのに "Next up" のままだった)。ブリーフ起草前に接地する規律がこれを捕捉した。round-count 上 5+ に達した論点は本 session には無し (Phase B は 2 round の壁打ちで確定)。`CLAUDE.md` / `AGENTS.md` への追加提案は不要。
