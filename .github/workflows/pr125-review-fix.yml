name: PR125 review fix

on:
  push:
    branches:
      - claude/weekly-report-jun29-jul3-gw058t
    paths:
      - .github/workflows/pr125-review-fix.yml

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - name: Check out PR branch
        uses: actions/checkout@v4
        with:
          ref: claude/weekly-report-jun29-jul3-gw058t
          fetch-depth: 0

      - name: Apply review fixes and regression tests
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace_once(path: str, old: str, new: str) -> None:
              p = Path(path)
              text = p.read_text(encoding="utf-8")
              if old not in text:
                  raise SystemExit(f"replacement anchor not found in {path}")
              p.write_text(text.replace(old, new, 1), encoding="utf-8")

          automation = "src/ugh_quantamental/fx_protocol/automation.py"
          old = '''                cu_existing_outcome = _CuFxOER.load_fx_outcome_record(session, cu_outcome_id)
                if cu_existing_outcome is not None:
                    cu_existing_evals = _CuFxOER.load_fx_evaluation_batch(session, cu_outcome_id)
                    if (
                        cu_existing_evals is not None
                        and len(cu_existing_evals) == EXPECTED_DAILY_BATCH_SIZE
                    ):
                        # Already recovered by a prior run — idempotent no-op,
                        # not re-reported.
                        continue

                cu_request = build_outcome_request_for_window(
                    window,
                    pair=config.pair,
                    market_data_provenance=snapshot.market_data_provenance,
                    schema_version=config.schema_version,
                    protocol_version=config.protocol_version,
                )
                cu_result = run_daily_outcome_evaluation_workflow(session, cu_request)
'''
          new = '''                cu_existing_outcome = _CuFxOER.load_fx_outcome_record(session, cu_outcome_id)
                cu_existing_evals = None
                if cu_existing_outcome is not None:
                    cu_existing_evals = _CuFxOER.load_fx_evaluation_batch(session, cu_outcome_id)

                cu_existing_complete = (
                    cu_existing_outcome is not None
                    and cu_existing_evals is not None
                    and len(cu_existing_evals) == EXPECTED_DAILY_BATCH_SIZE
                )
                cu_history_complete = False
                if config.write_csv_exports:
                    cu_history_dir = os.path.join(
                        config.csv_output_dir,
                        "history",
                        window.window_start_jst.strftime("%Y%m%d"),
                        cu_batch_id,
                    )
                    cu_history_complete = os.path.isfile(
                        os.path.join(cu_history_dir, "outcome.csv")
                    ) and os.path.isfile(os.path.join(cu_history_dir, "evaluation.csv"))

                if cu_existing_complete and (
                    not config.write_csv_exports or cu_history_complete
                ):
                    # Fully recovered and already published — idempotent no-op.
                    continue

                if cu_existing_complete:
                    # Database recovery succeeded on an earlier run but history
                    # publication did not. Reuse persisted records and retry only
                    # the missing file publication; never duplicate evaluations.
                    assert cu_existing_outcome is not None
                    assert cu_existing_evals is not None
                    cu_outcome = cu_existing_outcome
                    cu_evaluations = cu_existing_evals
                else:
                    cu_request = build_outcome_request_for_window(
                        window,
                        pair=config.pair,
                        market_data_provenance=snapshot.market_data_provenance,
                        schema_version=config.schema_version,
                        protocol_version=config.protocol_version,
                    )
                    # Repository workflows flush internally. A failed flush puts
                    # SQLAlchemy's current transaction into a failed state, so each
                    # catch-up candidate gets its own savepoint. Rolling back that
                    # savepoint leaves today's forecast and the outer transaction
                    # usable for later candidates and Step 5 exports.
                    with session.begin_nested():
                        cu_result = run_daily_outcome_evaluation_workflow(session, cu_request)
                    cu_outcome = cu_result.outcome
                    cu_evaluations = cu_result.evaluations
'''
          replace_once(automation, old, new)
          replace_once(
              automation,
              '''                    cu_outcome_csv_path = export_daily_outcome_csv(
                        cu_result.outcome,
''',
              '''                    cu_outcome_csv_path = export_daily_outcome_csv(
                        cu_outcome,
''',
          )
          replace_once(
              automation,
              '''                    cu_evaluation_csv_path = export_daily_evaluation_csv(
                        cu_result.evaluations,
''',
              '''                    cu_evaluation_csv_path = export_daily_evaluation_csv(
                        cu_evaluations,
''',
          )
          replace_once(
              automation,
              '''                        outcome_id=cu_result.outcome.outcome_id,
                        evaluation_count=len(cu_result.evaluations),
''',
              '''                        outcome_id=cu_outcome.outcome_id,
                        evaluation_count=len(cu_evaluations),
''',
          )

          catchup_tests = "tests/fx_protocol/test_outcome_catchup.py"
          marker = '''    def test_missing_forecast_batch_skipped_not_raised(self) -> None:
'''
          insertion = '''    def test_failed_catchup_flush_isolated_from_outer_transaction(self) -> None:
        """A flush failure inside catch-up must not poison today's outer transaction."""
        session = self._make_session()
        try:
            with tempfile.TemporaryDirectory() as tmpdir:
                cfg = FxDailyAutomationConfig(
                    run_outcome_evaluation=True,
                    run_forecast_generation=True,
                    write_csv_exports=True,
                    csv_output_dir=tmpdir,
                )
                self._run(session, 20, cfg)
                session.commit()

                from sqlalchemy import event
                from ugh_quantamental.fx_protocol import outcomes as outcomes_module

                real_workflow = outcomes_module.run_daily_outcome_evaluation_workflow

                def fail_during_flush(active_session, request):
                    @event.listens_for(active_session, "before_flush", once=True)
                    def _raise_once(*_args, **_kwargs):
                        raise RuntimeError("synthetic catch-up flush failure")

                    return real_workflow(active_session, request)

                # Day2 is missing, so Day3's immediate outcome is skipped and
                # the patched workflow is reached only by Step 4b catch-up.
                with patch.object(
                    outcomes_module,
                    "run_daily_outcome_evaluation_workflow",
                    side_effect=fail_during_flush,
                ):
                    _, r3 = self._run(session, 22, cfg)

                # The failed catch-up was swallowed, but Step 5 still ran and the
                # session remains usable/committable because the failure rolled
                # back only the nested savepoint.
                assert r3.forecast_created is True
                assert r3.catchup_windows == ()
                session.commit()

                from ugh_quantamental.persistence.repositories import FxForecastRepository

                current = FxForecastRepository.load_fx_forecast_batch(
                    session, r3.forecast_batch_id
                )
                assert current is not None
                assert len(current.forecasts) == 7
        finally:
            session.close()

    def test_rerun_repairs_missing_history_after_db_recovery(self) -> None:
        """Persisted DB evaluations must not suppress retry of missing history CSVs."""
        session = self._make_session()
        try:
            with tempfile.TemporaryDirectory() as tmpdir:
                cfg = FxDailyAutomationConfig(
                    run_outcome_evaluation=True,
                    run_forecast_generation=True,
                    write_csv_exports=True,
                    csv_output_dir=tmpdir,
                )
                self._run(session, 20, cfg)
                session.commit()
                _, first = self._run(session, 22, cfg)
                session.commit()
                assert len(first.catchup_windows) == 1
                cu = first.catchup_windows[0]

                history_dir = os.path.join(
                    tmpdir,
                    "history",
                    cu.window_start_jst.strftime("%Y%m%d"),
                    cu.forecast_batch_id,
                )
                eval_path = os.path.join(history_dir, "evaluation.csv")
                assert os.path.isfile(eval_path)
                os.remove(eval_path)

                _, retry = self._run(session, 22, cfg)
                session.commit()
                assert len(retry.catchup_windows) == 1
                assert retry.catchup_windows[0].outcome_id == cu.outcome_id
                assert os.path.isfile(eval_path)

                from ugh_quantamental.persistence.repositories import (
                    FxOutcomeEvaluationRepository,
                )

                evals = FxOutcomeEvaluationRepository.load_fx_evaluation_batch(
                    session, cu.outcome_id
                )
                assert evals is not None
                assert len(evals) == 7
        finally:
            session.close()

'''
          replace_once(catchup_tests, marker, insertion + marker)

          alert = "scripts/run_fx_price_alert.py"
          old = '''def fetch_issue_comment_bodies(repo: str, token: str, issue_number: int) -> list[str]:
    url = f"{GITHUB_API}/repos/{repo}/issues/{issue_number}/comments?per_page=100"
    comments = _github_request("GET", url, token) or []
    return [c.get("body", "") for c in comments]  # type: ignore[union-attr]
'''
          new = '''def fetch_issue_comment_bodies(repo: str, token: str, issue_number: int) -> list[str]:
    """Fetch every issue-comment body oldest-first, following REST pagination."""
    bodies: list[str] = []
    page = 1
    while True:
        url = (
            f"{GITHUB_API}/repos/{repo}/issues/{issue_number}/comments"
            f"?per_page=100&page={page}"
        )
        comments = _github_request("GET", url, token) or []
        bodies.extend(c.get("body", "") for c in comments)  # type: ignore[union-attr]
        if len(comments) < 100:  # type: ignore[arg-type]
            break
        page += 1
    return bodies


def fetch_issue_state_bodies(repo: str, token: str, issue: dict) -> list[str]:
    """Return state-bearing Issue text in chronological source order.

    The initial state marker is written into the Issue body when the tracking
    issue is first created. Comments are appended afterwards, so parsing body
    first and then all paginated comments makes the newest valid marker win.
    """
    bodies = [str(issue.get("body") or "")]
    bodies.extend(fetch_issue_comment_bodies(repo, token, int(issue["number"])))
    return bodies
'''
          replace_once(alert, old, new)
          replace_once(
              alert,
              '''    prev_comments = fetch_issue_comment_bodies(repo, token, issue["number"]) if issue else []
    prev_state = parse_alert_state(prev_comments)
''',
              '''    prev_state_bodies = fetch_issue_state_bodies(repo, token, issue) if issue else []
    prev_state = parse_alert_state(prev_state_bodies)
''',
          )

          alert_tests = "tests/ops/test_run_fx_price_alert.py"
          marker = '''# ---------------------------------------------------------------------------
# read_forecast_meta (local filesystem I/O — no network)
# ---------------------------------------------------------------------------
'''
          insertion = '''# ---------------------------------------------------------------------------
# GitHub Issue state I/O
# ---------------------------------------------------------------------------


class TestGitHubIssueStateIO:
    def test_initial_issue_body_restores_state_before_any_comments(self, monkeypatch) -> None:
        initial = fxa.encode_state_marker({"move": True, "gap": False})
        monkeypatch.setattr(fxa, "fetch_issue_comment_bodies", lambda *_args, **_kwargs: [])
        bodies = fxa.fetch_issue_state_bodies(
            "owner/repo", "token", {"number": 7, "body": "created\\n" + initial}
        )
        assert fxa.parse_alert_state(bodies) == {"move": True, "gap": False}

    def test_comment_fetch_follows_pagination_and_newest_marker_wins(self, monkeypatch) -> None:
        calls: list[str] = []
        older = fxa.encode_state_marker({"move": False})
        newer = fxa.encode_state_marker({"move": True, "gap": True})
        page1 = [{"body": older}] + [{"body": "noise"} for _ in range(99)]
        page2 = [{"body": newer}]

        def fake_request(method, url, token, body=None, timeout=30):
            calls.append(url)
            return page1 if "page=1" in url else page2

        monkeypatch.setattr(fxa, "_github_request", fake_request)
        bodies = fxa.fetch_issue_comment_bodies("owner/repo", "token", 9)
        assert len(bodies) == 101
        assert calls == [
            f"{fxa.GITHUB_API}/repos/owner/repo/issues/9/comments?per_page=100&page=1",
            f"{fxa.GITHUB_API}/repos/owner/repo/issues/9/comments?per_page=100&page=2",
        ]
        assert fxa.parse_alert_state(bodies) == {"move": True, "gap": True}


'''
          replace_once(alert_tests, marker, insertion + marker)
          PY

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install pytest 'ruff==0.15.8'

      - name: Focused lint and tests
        run: |
          ruff check src/ugh_quantamental/fx_protocol/automation.py scripts/run_fx_price_alert.py tests/fx_protocol/test_outcome_catchup.py tests/ops/test_run_fx_price_alert.py
          pytest -q tests/fx_protocol/test_outcome_catchup.py tests/ops/test_run_fx_price_alert.py
          git diff --check

      - name: Commit final tree without temporary workflow
        run: |
          rm .github/workflows/pr125-review-fix.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(fx): address PR125 catch-up and alert review findings"
          git push origin HEAD:claude/weekly-report-jun29-jul3-gw058t
