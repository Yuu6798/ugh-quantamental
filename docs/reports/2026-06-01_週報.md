# 週報: 2026-06-01 (月) 〜 2026-06-05 (金)

対象リポジトリ: `Yuu6798/ugh-quantamental`
集計対象: 全ブランチの commit / merge ログ + セッションメモリ (`.claude/memory/`)
作成日: 2026-06-06

---

## サマリー

- 2026-05 engine review (P0–P4 + Phase B) を **全フェーズ完走・クローズ**。
- Milestone 18 (FX Monthly Review) が **既存実装済み**であることを確認し、`PLANS.md` を実態同期。
- マージ PR: **5 本** (#109, #110, #111, #112, #113)。週前半 (6/1–6/2) に集中。
- コード変動: **29 files / +1,277 / −135**。engine 出力境界の調整とテスト増強が中心。
- engine default を **v2.0 → v2.3** まで段階 bump。
- 活動は 6/1–6/2 に集中し、**6/3–6/5 はコミットなし** (engine review クローズ後の区切り)。

---

## マージされた PR 一覧

| PR | 種別 | 内容 | engine_version |
|---|---|---|---|
| #109 | feat | ENGINE-P2B state classifier sharpening (二段 softmax T=0.5 / 0.12, fire weighted-sum + catalyst floor) | → v2.1 |
| #110 | feat | ENGINE-P3A rare FLAT epsilon (UGH variant 限定 fixed 3.0bp, `ratio=0.0`/`floor=3.0`) | → v2.2 |
| #111 | feat | ENGINE-P3B variant-specific expected_range (projection width 一本化, `range_width_scale=2.0`, `ugh_v2_ensemble` 撤去) | → v2.3 |
| #112 | docs | ENGINE-P4 conviction 意味論明文化 (reliability + magnitude scaler の二重役割, §8 dormant↔magnitude を Option B で記録) | 据え置き |
| #113 | docs | M18 確認 / PLANS.md 実態同期 (Milestone 18 を完了表へ, Next up 更新) | 据え置き |

> 補足: #107 (range_hit ensemble) / #108 (engine_version auto-stratify) は 6/1 早朝に先行マージ済み (前週セッション扱い)。本週はその続きから P2B〜P4 + Phase B を締めた。

---

## 工程の流れ

1. **engine review P2B/P3A の締め** (6/1) — state classifier の鋭利化と rare FLAT epsilon。bump 対象は出力境界が変わる箇所のみに限定。
2. **Phase B (range per-variant)** (6/1 S2) — range の出どころを 3 案比較し projection width 一本化 (案A) を採用。非 FLAT は expected close に recenter、FLAT は spot 中心対称。replay 較正で range_hit 71.7% に着地。
3. **Phase 4 (conviction spec)** (6/1 S2) — conviction の二重役割を spec/docstring に明記、dormant↔magnitude の分離は売買層へ委譲 (Option B)。docs-only。
4. **Milestone 18 検証 + PLANS 同期** (6/1 S2 → 6/2 #113) — monthly review が既存実装済み (891行 + spec 2本 + workflow 2本 + test 1745行) と判明。end-to-end スモークで確認し PLANS を実態化。

---

## コード変動 (6/1–6/2, 29 files / +1,277 / −135)

| 領域 | 主な変更 |
|---|---|
| `engine/` | `state.py` (+37) 二段温度 classifier, `projection.py` rare FLAT epsilon, `*_models.py` パラメータ追加 |
| `fx_protocol/` | `forecasting.py` (+64) per-variant range, `report_window.py` (+52) stratify, `monthly_review.py` 整理 |
| `docs/specs/` | `fx_ugh_engine_v2.md` (+146) range/conviction/§8, `ugh_state_engine_v1.md` fire gate 意図 |
| `tests/` | `test_forecasting.py` (+238), `test_monthly_review*` (+223), `test_report_window.py` (+92), `test_weekly_report_v2.py` (+123) 他 — 計 ~1,000 行の test 増 |
| memory/PLANS | session log, STATUS sweep, PLANS M18 同期 |

テスト増分 (約 +1,000 行) が実装増分を上回り、振る舞い変更を test/spec へ外部化する規律が機能した。

---

## ハイライト / 効いたパターン

- **接地してから壁打ち**: コードで現状を確認してから設計 fork を提示し、churn ゼロで Phase B を確定。
- **acceptance 二層化**: Hard=pytest / Soft=replay 報告に分け、脆い rate threshold を unit test に固定せず、PR #111 は 0 review round で landing。
- **grounding gate が drift を捕捉**: `/new-brief` の pre-flight が stale 参照 2 件 (epsilon default, 行番号移動) を検出。

## 修正・訂正の記録

- PR #111 レビューで「scope 外変更」と誤検知 → 原因はローカル `main` が stale。`origin/main` 基準で diff し直して訂正。
- M18 を「未着手」と思い込まず PLANS の "Next up" を疑い接地した結果、既存完全実装を発見。

---

## 未着手 / 次の発行順序 (週末時点)

1. **governance spec `Status: Draft` バナーの実態確認** — `docs/specs/fx_monthly_governance_v1.md` は実装+test+workflow 完備なのにバナーが `Draft`。実態確認のうえ更新検討。
2. **売買 / execution レイヤーの planning doc 起草** — 次の大方向。engine 出力 (state lifecycle / conviction / expected_close_change_bp / per-variant expected_range) を入力にした position sizing。大規模新スコープのため `docs/specs/` の planning から着手。

---

## メモ

- 6/3–6/5 のコミット空白は、engine review クローズ後・新スコープ (売買レイヤー) planning 着手前の区切りに対応。
- engine 側の前提整備 (state / range / conviction の意味論明文化) は完了。次フェーズは新規大スコープとして planning から始める段階。
